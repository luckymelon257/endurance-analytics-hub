/**
 * Standard envelope returned by service methods that perform an action and
 * communicate a single human-readable status string back to the caller.
 */
export interface OperationMessage {
  message: string
}
